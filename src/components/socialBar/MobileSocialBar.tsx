import React, { FC } from 'react';
import styled from '@emotion/styled';

const Container = styled.div`

`;

export const MobileSocialBar: FC = props => {
    return <Container>

    </Container>;
};
